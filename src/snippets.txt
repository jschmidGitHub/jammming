// Get Spotify User Profile
const getSearchResults = async (searchQuery, page) => {

    const endpoint = '/v1/me';
    const url = `${baseUrl}${endpoint}`;

    const profileResponse = await fetch(url, {
      headers: {
        Authorization: `Bearer ${accessToken}`
      }
    });

    if (!profileResponse.ok) throw new Error('Network error while querying users profile at /v1/me');
    return profileResponse;
  };

endpoint = `/v1/tracks/${trackId}`;

  // Determine when the form should be disabled
  const isSearchFormDisabled = 
    (selectedOption === 'album' && artistId) ||
    (selectedOption === 'track' && albumId);

      useEffect(() => {
     console.log('Form disabled?: ', props.isSearchFormDisabled);
  }, [props.isSearchFormDisabled]);